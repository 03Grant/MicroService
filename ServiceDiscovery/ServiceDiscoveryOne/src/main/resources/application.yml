################ Eureka?? ################
server:
  port: 8761

eureka:
  instance:
    hostname: eureka1
  client:
    #register-with-eureka: false
    #fetch-registry: false
    service-url:
      # 两个端口分别为8761与8762
      defaultZone: http://eureka2:8762/eureka
